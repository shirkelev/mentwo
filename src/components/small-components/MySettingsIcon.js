import React from 'react';
import IconButton from '@mui/material/IconButton';
import SettingsIcon from '@mui/icons-material/Settings';

const MySettingsIcon = () => {
  return (
    <IconButton>
      <SettingsIcon />
    </IconButton>
  );
};

export default MySettingsIcon;
